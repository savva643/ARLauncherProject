# Объяснение и исправления

## 1. Текст в UI и глифы

### Что такое глифы?
**Глифы** - это простые символы, которые рисуются через простую растровую систему (5x7 пикселей). 
В коде используется простая система рендеринга текста без библиотеки шрифтов.

### Какой текст в UI?
В углу экрана отображаются следующие UI элементы:
- "📷 Camera: Waiting..." - статус камеры
- "📍 Position: (0, 0, 0)" - текущая позиция
- "⚡ IMU: No data" - данные IMU
- "📡 iPhone: Disconnected" - статус подключения
- "🔌 Connect iPhone" - кнопка подключения

### Проблема с квадратиками
Эти тексты содержат:
- **Эмодзи** (📷, 📍, ⚡, 📡, 🔌) - это UTF-8 символы, которые не поддерживаются простой системой глифов
- **Английские буквы** - должны отображаться правильно
- **Русские символы** - не поддерживаются (нет глифов для них)

**Решение:** Простая система глифов поддерживает только ASCII символы (A-Z, 0-9, некоторые знаки). 
Для полной поддержки эмодзи и русских символов нужна библиотека FreeType или аналогичная.

## 2. JPEG декодирование и стрим

### Как работает декодирование?
1. **TurboJPEG** используется для декодирования JPEG кадров
2. **Асинхронное декодирование** - каждый кадр декодируется в отдельном потоке через `QThreadPool`
3. **Стрим обработка** - `decodeJPEGAsync()` не блокирует основной поток
4. **Результат** - декодированное изображение передается через сигнал `frameDecoded`

### Проверка:
✅ JPEG правильно декодируется через TurboJPEG
✅ Используется асинхронное декодирование (не блокирует)
✅ Результат конвертируется в RGB888 формат
✅ Используется `glTexSubImage2D` для обновления текстуры (быстро, стрим)

**Все правильно!** Декодирование работает как стрим без задержек.

## 3. Фигуры (шар и кубы)

### Что было исправлено:
1. **Куб** - уже был правильным (24 вершины, 36 индексов, 6 граней)
2. **Сфера** - была неправильной (использовался неправильный икосаэдр)
   - **Исправлено:** Создана правильная сфера через параметрическое уравнение
   - 32 сегмента по горизонтали, 16 колец по вертикали
   - Правильные нормали и UV координаты
3. **Stride в vertex attributes** - был неправильный (6 вместо 8)
   - **Исправлено:** stride = 8 (pos(3) + normal(3) + uv(2) = 8 floats)

### Сцена:
- **1 куб** (квадрат) - в центре
- **2 сферы** (круга) - слева и справа от куба

**Теперь фигуры правильные!** Куб будет выглядеть как куб, сферы как гладкие шары.

## 4. Видеофон

### Проблема:
Видеофон не отображался, потому что `renderVideoBackground()` вызывался асинхронно 
и рендерил кадр только когда приходил новый кадр, но не сохранял текстуру для постоянного отображения.

### Исправление:
1. `renderVideoBackground()` теперь только **обновляет текстуру** (не рендерит)
2. Добавлен метод `renderStoredVideoBackground()` который **рендерит сохраненную текстуру** в каждом кадре
3. В `render()` вызывается `renderStoredVideoBackground()` перед 3D объектами

**Теперь видеофон должен отображаться постоянно!**

## Итог:

✅ **Логи IMU** - вернул логи каждые 60 кадров с полной информацией 6DOF
✅ **Движение камеры** - камера обновляется напрямую в колбэке LensEngine
✅ **Видеофон** - добавлен метод для рендеринга сохраненной текстуры в каждом кадре
✅ **Фигуры** - исправлена сфера и stride в vertex attributes
✅ **JPEG стрим** - уже работает правильно (асинхронное декодирование)
⚠️ **Кодировка текста** - для полной поддержки нужна библиотека FreeType

