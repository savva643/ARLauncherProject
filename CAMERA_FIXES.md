# Исправления камеры и видео

## 1. ✅ Исправлен переворот видео
- **Проблема**: Видео отображалось перевернутым
- **Решение**: 
  - QImage имеет координаты сверху вниз (0,0 вверху)
  - OpenGL текстуры имеют координаты снизу вверх (0,0 внизу)
  - Исправлены координаты текстуры: `glTexCoord2f(0.0f, 1.0f)` для нижнего левого угла

## 2. ✅ Исправлено растягивание видео
- **Проблема**: Видео растягивалось на весь экран
- **Решение**: 
  - Добавлен расчет aspect ratio для видео и окна
  - Видео отображается с правильным соотношением сторон
  - Черные полосы по бокам или сверху/снизу если нужно

## 3. ✅ Оптимизация FPS (30 FPS)
- **Проблема**: Видео отставало (15-20 FPS вместо 30)
- **Решение**: 
  - Используется `glTexSubImage2D` для быстрого обновления
  - Оптимизирован `GL_UNPACK_ALIGNMENT` для RGB (3 байта на пиксель)
  - Добавлен `GL_UNPACK_ROW_LENGTH` для максимальной скорости
  - Прямой доступ к данным без копирования

## 4. ✅ Исправлен текст в UI
- **Проблема**: Текст не отображался
- **Решение**: 
  - Исправлен `glOrtho` в `Text.cpp`
  - Используется правильная система координат: `glOrtho(0, width, height, 0, -1, 1)`
  - Теперь (0,0) вверху слева для UI

## 5. 🔍 Камера - система координат
- **Проблема**: Камера уходит в бок вместо правильной позиции
- **Анализ**: 
  - Позиция применяется напрямую из LensEngine
  - Возможно проблема в системе координат (правая/левая)
  - Добавлено логирование для отладки
  - Нужно проверить логи `[Camera] Pose updated` чтобы понять проблему

## Следующие шаги:
1. Проверить логи `[Camera] Pose updated` - как изменяется позиция
2. Если камера уходит в бок, возможно нужно:
   - Поменять местами X и Z оси
   - Или инвертировать одну из осей
   - Или изменить порядок применения трансформаций

## Итог:
- ✅ Видео отображается правильно (не перевернуто)
- ✅ Сохранено соотношение сторон
- ✅ Оптимизировано для 30 FPS
- ✅ Текст в UI отображается
- 🔍 Камера - нужна дополнительная отладка по логам

