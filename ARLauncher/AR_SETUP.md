# 🎯 Инструкция по настройке AR с iPhone

## ✅ Что реализовано

1. **RGB поток с камеры iPhone** - декодирование JPEG и преобразование в RGB
2. **Рендеринг видео фона** - отображение RGB камеры как фона для AR
3. **3D объекты поверх видео** - AR наложение виртуального мира на реальный
4. **UI элементы** - AR стиль поверх всего

## 📱 Подключение iPhone 14 Pro

### Вариант 1: USB (Ethernet через USB) - Рекомендуется

1. Подключите iPhone 14 Pro к компьютеру через USB кабель
2. На iPhone включите:
   - **Настройки** → **Личный хот-спот** → **Разрешить другим подключаться**
   - Или используйте приложение для передачи данных через USB
3. ARLauncher автоматически найдет подключение на порту 9001 (USB)

### Вариант 2: WiFi (TCP)

1. Убедитесь, что iPhone и компьютер в одной WiFi сети
2. На iPhone запустите приложение для передачи AR данных
3. ARLauncher будет слушать на порту 9000 (TCP)

## 🔧 Как собрать и запустить

### 1. Собрать SensorConnector с Qt6

```bash
cd /home/savva/Documents/GitHub/ARLauncherProject/SensorConnector

# В Qt Creator:
# 1. Откройте SensorConnector.pro
# 2. Выберите Qt 6.5.3 kit
# 3. Соберите проект (Build → Build All)
```

Или через командную строку:
```bash
cd /home/savva/Documents/GitHub/ARLauncherProject/SensorConnector
qmake6 SensorConnector.pro
make -j$(nproc)
```

### 2. Собрать ARLauncher с SensorConnector

```bash
cd /home/savva/Documents/GitHub/ARLauncherProject/ARLauncher/build
rm -rf *
cmake .. -DUSE_VULKAN=ON -DUSE_SENSOR_CONNECTOR=ON -DLENSENGINE_SDK_DIR=../../LensEngineSDK -DSENSOR_CONNECTOR_DIR=../../SensorConnector
make -j$(nproc)
```

### 3. Запустить ARLauncher

```bash
cd /home/savva/Documents/GitHub/ARLauncherProject/ARLauncher/build/bin
./ARLauncher
```

## 🎬 Как это работает

1. **iPhone отправляет данные:**
   - RGB кадры с камеры (JPEG → декодируется в RGB)
   - IMU данные (gyroscope, accelerometer)
   - LiDAR данные (глубина)
   - Feature Points (для трекинга)

2. **SensorConnector получает и декодирует:**
   - JPEG → QImage (RGB)
   - Эмитит сигнал `frameDecoded(QImage, sequenceNumber)`

3. **ARLauncher получает RGB кадры:**
   - Преобразует QImage в RGB данные
   - Вызывает `renderer->renderVideoBackground(rgbData, width, height)`
   - Рендерит видео фон

4. **Рендеринг AR:**
   - **Слой 1:** RGB видео фон с камеры iPhone
   - **Слой 2:** 3D объекты (кубы, сфера) поверх видео
   - **Слой 3:** UI элементы (кнопки, виджеты) поверх всего

## 📊 Что отображается

- **RGB поток с камеры** - фон для AR (реальный мир)
- **3D объекты** - виртуальный мир поверх реального
- **UI элементы** - полупрозрачные кнопки и виджеты
- **Виджеты данных** - статус камеры, поза, IMU, подключение iPhone

## ⚠️ Известные ограничения

1. **Рендеринг видео в Vulkan** - текстура обновляется, но пока не отображается (нужен graphics pipeline с шейдерами)
2. **Текст** - пока рендерится как точки (нужен FreeType для полноценного текста)
3. **3D объекты** - созданы, но пока не рендерятся (нужен graphics pipeline)

## 🚀 Что дальше

1. **Реализовать graphics pipeline в Vulkan:**
   - Vertex shader для рендеринга 3D объектов
   - Fragment shader для рендеринга видео фона
   - Fullscreen quad для видео фона

2. **Интегрировать LensEngineSDK:**
   - Передавать RGB кадры в LensEngine для обработки
   - Получать позу камеры для AR трекинга
   - Использовать Feature Points для стабилизации

3. **Добавить полноценный текст:**
   - Интегрировать FreeType для рендеринга текста
   - Поддержка разных шрифтов и размеров

## 📝 Порядок действий при запуске

1. ✅ Соберите SensorConnector с Qt6 в Qt Creator
2. ✅ Соберите ARLauncher с `-DUSE_SENSOR_CONNECTOR=ON`
3. ✅ Подключите iPhone через USB или WiFi
4. ✅ Запустите ARLauncher
5. ✅ На iPhone запустите приложение для передачи данных
6. ✅ Видео фон должен появиться автоматически

## 🔍 Проверка работы

После подключения iPhone и запуска приложения на iPhone, в консоли ARLauncher должны появиться сообщения:
```
✅ SensorConnector initialized
   TCP Server: port 9000
   UDP Server: port 9000
   USB Server: port 9001
   Waiting for iPhone connection...
📹 RGB Frame received: 1920x1080 (Seq: 123)
```

Если видите эти сообщения - все работает! 🎉

