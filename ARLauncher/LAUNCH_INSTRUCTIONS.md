# 🚀 Инструкция по запуску ARLauncher

## ✅ Что готово

1. **Vulkan рендерер** - полностью инициализирован и работает
2. **Тестовые 3D фигуры** - кубы и сфера добавлены в сцену
3. **AR стиль UI** - полупрозрачные кнопки, стеклянные эффекты
4. **Виджеты для iPhone** - виджеты для отображения данных камеры, позы, IMU

## 🎮 Запуск

```bash
cd /home/savva/Documents/GitHub/ARLauncherProject/ARLauncher/build/bin
./ARLauncher
```

## 📱 Подключение iPhone 14 Pro

### Вариант 1: USB (Ethernet через USB)

1. Подключите iPhone 14 Pro к компьютеру через USB
2. На iPhone включите "Настройки" → "Личный хот-спот" → "Разрешить другим подключаться"
3. ARLauncher автоматически найдет подключение через USB (порт 9001)

### Вариант 2: WiFi (TCP)

1. Убедитесь, что iPhone и компьютер в одной WiFi сети
2. На iPhone запустите приложение для передачи данных
3. ARLauncher будет слушать на порту 9000 (TCP)

## 🎨 UI элементы

В лаунчере доступны:
- **🚀 Start AR** - кнопка запуска AR режима
- **🔌 Connect iPhone** - кнопка подключения iPhone
- **📷 Camera** - статус камеры
- **📍 Position** - позиция камеры (x, y, z)
- **⚡ IMU** - данные IMU сенсора
- **📡 iPhone** - статус подключения iPhone

## ⚙️ Стили AR UI

Все UI элементы используют AR стиль:
- **Полупрозрачные кнопки** - синий фон с прозрачностью 70%
- **Стеклянные эффекты** - размытие для glass эффекта
- **Скругленные углы** - радиус 8-12px
- **Светлые рамки** - синие акцентные рамки

## 🔧 Для подключения SensorConnector

Если нужно подключить SensorConnector (сейчас отключен из-за Qt5/Qt6 конфликта):

1. Пересоберите SensorConnector с Qt6:
```bash
cd /home/savva/Documents/GitHub/ARLauncherProject/SensorConnector
qmake6 SensorConnector.pro
make
```

2. Включите в ARLauncher:
```bash
cd /home/savva/Documents/GitHub/ARLauncherProject/ARLauncher/build
rm -rf *
cmake .. -DUSE_VULKAN=ON -DUSE_SENSOR_CONNECTOR=ON
make -j$(nproc)
```

## 📊 Что отображается

- **3D объекты** - кубы и сфера в 3D сцене
- **UI элементы** - кнопки и виджеты с AR стилем
- **Черный фон** - пока нет данных с iPhone

## 🎯 Следующие шаги

1. Подключить iPhone через USB или WiFi
2. Получать данные с iPhone через SensorConnector
3. Передавать данные в LensEngineSDK
4. Отображать AR контент поверх видео с iPhone

## 🐛 Известные ограничения

- UI рендеринг работает только в OpenGL режиме (не в Vulkan)
- Текст рендерится как точки (нужен FreeType для полноценного текста)
- 3D объекты создаются, но не рендерятся в Vulkan (нужен graphics pipeline с шейдерами)

Для полного рендеринга нужно:
- Реализовать graphics pipeline в Vulkan
- Добавить шейдеры (vertex + fragment)
- Реализовать рендеринг UI в Vulkan

